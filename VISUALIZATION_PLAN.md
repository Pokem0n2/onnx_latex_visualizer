# LaTeX OCR 全流程可视化计划

## 当前状态
- ✅ 拖拽图片上传
- ✅ 图像预处理（6步动画）
- ✅ Encoder 特征图热力图（已实现基础版）
- ✅ 识别结果打字机动画
- ✅ 自动滚动

---

## 阶段1：特征图可视化增强

### 1.1 原始 12×12 数值网格
- 把 144 个特征值可视化为灰度格子
- 每个格子亮度 = 该位置激活强度
- 动画：格子逐个亮起

### 1.2 数值→颜色映射动画
- 展示归一化过程（min→0, max→255）
- 逐格变色（蓝→青→绿→黄→红）
- 显示数值标签

### 1.3 热力图最终展示
- 放大到 200×200 可见尺寸
- 动画：从模糊到清晰

---

## 阶段2：归一化过程可视化

### 2.1 显示原始像素值分布
- 直方图展示灰度分布

### 2.2 归一化计算过程
- 公式：`(x - mean) / std`
- mean = 0.7931, std = 0.1738
- 动画：数值变化过程

### 2.3 转换后分布
- 展示归一化后的数值范围

---

## 阶段3：神经网络层传递动画（可选，需较高复杂度）

### 3.1 12层 Encoder 可视化
- 显示 12 个卷积层
- 动画：数据逐层传递
- 每层显示输出 shape 变化

### 3.2 特征流动效果
- 图像 → 层1 → 层2 → ... → 特征图
- 类似水流流动效果

---

## 阶段4：Token 生成过程（可选）

### 4.1 Decoder 自回归可视化
- 每步显示：输入 token → 模型 → 输出概率分布
- Top-5 候选 token 及其概率
- 选中 token 的高亮效果

### 4.2 完整流程
- BOS → token1 → token2 → ... → EOS
- 实时显示已生成的部分

---

## 实现顺序

1. **阶段1.1** - 12×12 数值网格动画（先确认）
2. **阶段1.2** - 颜色映射动画
3. **阶段1.3** - 热力图放大动画
4. **阶段2** - 归一化过程（可选）
5. **阶段3** - NN层动画（可选，高复杂度）
6. **阶段4** - Token生成（可选）

---

建议先实现阶段1，测试效果后再决定后续。
